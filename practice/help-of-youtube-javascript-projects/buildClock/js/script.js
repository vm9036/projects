setInterval(setClock, 1000)

function setClock() {
    const currentDate = new Date()
    const secondRatio = currentDate.getSeconds() / 60;
    const secondRatio = currentDate.getSeconds() / 60;
    const secondRatio = currentDate.getSeconds() / 60;


}